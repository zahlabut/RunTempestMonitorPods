# Generated by Cursor AI Assistant
# https://cursor.sh

---
apiVersion: test.openstack.org/v1beta1
kind: Tempest
metadata:
  name: designate-tempest-test-2
  namespace: openstack
spec:
  # OpenStack credentials
  openStackConfigMap: openstack-config
  openStackConfigSecret: openstack-config-secret
  
  # Container image for tempest
  containerImage: quay.io/podified-antelope-centos9/openstack-tempest:current-podified
  
  # Tempest configuration
  tempestconfRun:
    create: true
    profile:
      debug: true
      verbose: true
  
  # Test configuration
  includeList: |
    designate_tempest_plugin.tests.api.v2.test_blacklists
    designate_tempest_plugin.tests.api.v2.test_pool
  
  # Parallel execution
  parallel: true
  workerFile: ""
  
  # Resource limits
  resources:
    requests:
      memory: "2Gi"
      cpu: "1000m"
    limits:
      memory: "4Gi"
      cpu: "2000m"

